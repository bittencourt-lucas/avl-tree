/*************************************************************
* GET MAX INT
* @author Lucas Bittencourt
* 
* Returns the biggest between two integers.
*
* @param a is the first integer
* @param b is the second integer
* @return the bigges of two integers
* ***********************************************************/

int GetMaxInt(int a, int b) {
  return (a > b) ? a : b;
}
